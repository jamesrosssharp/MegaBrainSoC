== What is this? ==

A simple python GUI for managing the ISA.xml file which defines the instruction set. Each instruction has C++ code elements which can be used to generate the disassembly and decode and execute the instruction.
